import type { Plugin } from 'vite';
import 'winston-daily-rotate-file';
export interface DevLoggerOptions {
    filename?: string;
    dirname?: string;
    datePattern?: string;
    maxFiles?: string;
    maxSize?: string;
}
export declare function devLogger(options?: DevLoggerOptions): Plugin;
